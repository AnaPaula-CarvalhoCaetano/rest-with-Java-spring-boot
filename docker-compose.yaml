version: '3.8'
services:
  api:
    build: .
    container_name: spring_api
    ports:
      - "8081:8080"        # host 8081, container 8080
    environment:
      SPRING_PROFILES_ACTIVE: h2   # ativa H2 dentro do container
