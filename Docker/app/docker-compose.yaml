version: '3.8'

services:
  api:
    build: ../..
    container_name: spring_api
    ports:
      - "8081:8080"
    environment:
      SPRING_PROFILES_ACTIVE: h2
    networks:
      - observability

networks:
  observability:
    external: true
